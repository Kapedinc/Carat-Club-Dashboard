declare namespace _default {
    export let title: string;
    export { Cell as component };
}
export default _default;
export namespace Default {
    namespace args {
        let secondLabel: string;
        let label: string;
        let type: string;
        let className: {};
        let ellipse: string;
        let hasSecondLabel: boolean;
        let frameClassName: {};
        let ellipseClassName: {};
    }
}
import { Cell } from ".";
//# sourceMappingURL=Cell.stories.d.ts.map